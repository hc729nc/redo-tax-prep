# Leave unset for local dev (defaults to http://localhost:8000) and for a
# single-service deployment where the backend serves this built frontend itself
# (defaults to same-origin relative requests). Only set this if the frontend is
# hosted separately from the backend.
VITE_API_BASE_URL=
